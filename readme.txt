For run tests:
1. Make sure, that you have tuned environment (adb, local variables, etc);
2. Run emulator;
3. Run Appium server;
4. Change properties in nativeTNG.xml and webTNG.xml, if you need it;
5. Run tests by maven profiles native or web.